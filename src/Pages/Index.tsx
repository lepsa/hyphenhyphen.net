import React from 'react';

export type IndexProps =
  {
  };

export const Index : React.FunctionComponent<IndexProps> = (props) => (
  <div>
    <p>
      Foo Bar
    </p>
  </div>
)

export default Index;